name: font
description_plain: "The font CSS property is either a shorthand property for setting font-style, font-variant, font-weight, font-size, line-height and font-family, or a way to set the element's font to a system font, using specific keywords."
description_html: "The <code>font</code> CSS property is either a shorthand property for setting <code>font-style</code>\n, <code>font-variant</code>\n, <code>font-weight</code>\n, <code>font-size</code>\n, <code>line-height</code>\n and <code>font-family</code>\n, or a way to set the element's font to a system font, using specific keywords."
javascript_property_name: font
